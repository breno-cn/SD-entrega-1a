syntax = "proto3";


message Response {
    int32 status = 1;
    string value = 2;
}

message ReadRequest {
    string key = 1;
}

service Hashtable {
    rpc read (ReadRequest) returns (Response);
}
